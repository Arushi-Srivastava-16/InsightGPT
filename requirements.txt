# Core dependencies
pydantic>=2.0.0
numpy

# LangChain ecosystem
langchain
langchain-community
langchain-core
langchain-experimental
langchain-openai
langchain-neo4j

# Database & vector stores
neo4j

# PDF processing (Streamlit Cloud compatible)
# Use fallback libraries first to avoid unstructured conflicts
pymupdf>=1.26.0
pypdf>=3.0.0
pdfminer.six
ml-dtypes>=0.5.0
# Unstructured is optional for now - can cause install issues
# unstructured[pdf]

# Data processing
json_repair

# Visualization (for Streamlit app)
streamlit
pandas
networkx
pyvis
plotly

# Configuration management
python-dotenv