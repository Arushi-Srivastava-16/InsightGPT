# Core dependencies
pydantic>=2.0.0
numpy>=1.21.0,<2.0.0

# LangChain ecosystem
langchain
langchain-community
langchain-core
langchain-experimental
langchain-openai
langchain-neo4j

# LLM providers
ollama

# Database & vector stores
neo4j
chromadb

# PDF processing (Streamlit Cloud compatible)
# Use fallback libraries first to avoid unstructured conflicts
pymupdf>=4.21.0
pypdf>=3.0.0
pdfminer.six
# Unstructured is optional, will fallback if not available
unstructured[pdf]
ml-dtypes>=0.5.0

# Image processing & OCR (Streamlit Cloud compatible)
opencv-python-headless>=4.8.0.76
pytesseract

# Data processing
json_repair

# Reference management
pyzotero

# Visualization (for Streamlit app)
streamlit
pandas
networkx
pyvis
plotly

# Configuration management
python-dotenv